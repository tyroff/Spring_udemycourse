<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- сканирование бинов через аннотации, указывается путь к папке для сканирования -->
    <context:component-scan base-package="org.example"/>

    <!--context:property-placeholder location="classpath:musicPlayer.properties"/>

    <bean id="testBean" class="org.example.TestBean">
        <constructor-arg value="Kirill"/>
    </bean>

    <bean id="musicBean" class="org.example.RockMusic"/>
    <bean id="musicBean2" class="org.example.ClassicMusic" init-method="doInit" destroy-method="doDestroy"/>
    <bean id="musicBean3" class="org.example.PopMusic"/>


    <bean id="musicPlayer" class="org.example.MusicPlayer">
        // constructor-arg ref="musicBean"
        <property name="music" ref="musicBean"/>
        <property name="name" value="${musicPlayer.name}"/>
        <property name="value" value="${musicPlayer.value}"/>
        <property name="musicList">
            <list>
                <ref bean="musicBean"/>
                <ref bean="musicBean2"/>
                <ref bean="musicBean3"/>
            </list>
        </property>
    </bean-->

</beans>